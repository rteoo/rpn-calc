# Audit verification — 2026-09-07

Base: `2e0e804`. Host: macOS Apple Silicon, Python 3.12.14, PySide6 6.11.2.
The only runtime edit removes unused SCI/ENG imports from the Qt backend;
both display modes remain implemented in the calculation engine.

## Executed checks

- Fresh local virtual environment; `python -m pip install -e ".[dev]"` passed.
  No manifest or lockfile was changed.
- `python tools/verify_core.py`: 1,873 passed, 12 skipped. All six gated modules
  reached 100% statement and branch coverage (1,350 statements, 554 branches).
- `python tools/smoke_macos.py --source`: cocoa, exposed window, 420 × 820,
  correct title and multi-resolution `.icns`; `SMOKE_OK`.
- `python -m compileall -q src tests tools`: passed.
- Focused Ruff F401 check of `src/rpncalc/backend.py`: passed, using an existing
  audit environment. Ruff is not a declared project dependency or a full lint gate.
- `git diff --check`: passed.

The source smoke used an external `sitecustomize` fixture to select INI-format
QSettings and redirect both scopes to a temporary directory before startup.
It otherwise executed the documented smoke command and real cocoa window;
the operator's calculator settings were not used as a test fixture.

## Verification limits

The skipped tests are conditional platform cases. Actual Windows calculator-key
dispatch, fresh frozen bundles, Developer ID signing/notarization, and iOS were
not exercised locally in this pass. No package or release was published.
The existing CI separately builds and smokes the macOS bundle and tests the
suite on Linux, Windows, and macOS; its result must be read on the PR's commit.

Earlier Windows size/timing measurements and fixed-defect narratives are kept
in [the historical notes](../history/2026-09-07-engineering-notes.md), with their
original wording and an explicit historical label. They are not new measurements.
